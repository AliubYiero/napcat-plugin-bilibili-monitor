# Bilibili 直播与动态监听

描述 Bilibili 监控插件的领域语言:监控哪些主播、直播间状态如何表示、何时推送通知,以及主播发布动态的解析与推送。

## Language

**用户角色 (UserRole)**:
消息发送者在权限体系中的身份,四档:`user`(普通用户, 含非好友私聊与群聊普通成员)、`admin`(群管理员, 仅群聊可达)、`privateUser`(机器人好友的私聊用户, 等同 admin 权限组)、`superAdmin`(配置于 adminUsers 名单的超级管理员)。优先级 superAdmin > privateUser > admin > user。私聊消息按 sub_type 区分: 好友(friend)为 privateUser 或 superAdmin, 非好友临时会话(group)降为 user; 不存在私聊 admin。

**帮助版本 (HelpVariant)**:
帮助指令输出的内容版本,由角色与会话类型共同决定,而非仅由角色决定:群聊超管与群管理员/私聊用户输出 Admin 版,仅私聊超管输出 SuperAdmin 版。_Avoid_: 按角色一一对应帮助版本

**群启用开关 (group enabled)**:
群级启用开关 (`GroupConfig.enabled`), 语义为默认开启: 未配置或字段缺失视为启用, 仅显式 `false` 禁用 (宽松判定 `enabled !== false`)。判定入口唯一 (`isGroupEnabled`), 消息入口最前端短路。修改入口当前仅 WebUI。
_Avoid_: 默认关闭, 散落读取 enabled

**主播 (uid)**:
被监控的 Bilibili 用户,以 mid 标识。插件对每个主播记录其直播间状态并推送变化,亦可监听其发布的动态。
_Avoid_: UP主, 用户, 房间号

**监控项 (BiliLiveMonitor)**:
一个主播及其绑定的推送目标列表。监控项集合构成"监控列表",是轮询的数据源。

**推送目标 (BiliLiveMonitorToInfo)**:
一条通知的接收方,是私聊(带 QQ 号)或群聊(带群号)。群聊目标可选携带`开播 @ 订阅`(mentionUsers),即该群对该主播开播时希望被 @ 的成员 QQ 号列表。
_Avoid_: 订阅, 关注

**开播 @ 订阅 (mention subscription)**:
群成员通过 `#bili live mention` 在群内为某主播设置的订阅:该主播开播时,推送通知会附加 @ 提醒该成员。按 (主播 uid, 群号) 分组,存储为该群推送目标上的 `mentionUsers` 列表;订阅只对未来开播生效,主播正在直播时设置不补发。同组成员可在 `#bili live unmention` 取消。

**直播间信息 (BiliLiveRoomInfo)**:
某个主播当前直播间的一份快照:房间号、直播状态、标题、分区、开播时间、主播名。

**直播状态 (live_status)**:
直播间是否在播,两态:`streaming`(正在直播)或 `offline`(未开播)。B站接口原始三态(0 未开播 / 1 直播中 / 2 轮播中)映射为:仅 1 → `streaming`,0 和 2 → `offline`(轮播中视为未开播)。
_Avoid_: 在线, 开播中

**分区 (area)**:
直播间的直播分区,由父分区 + 子分区组成,取自 B站新版分区字段。展示为复合文本 `父分区 - 子分区`(连字符两侧各一个空格);任一侧为空时只显示非空的那一侧,两侧相同时只显示子分区,两侧皆空显示 `未知分区`。
_Avoid_: 分类, 栏目

**变化事件 (ChangeEvent)**:
直播间状态变化事件,带变化类型和旧/新值。四种类型:开始直播、结束直播、修改标题、修改分区。结束时携带变更前的完整直播间信息,用于取开播时间等旧值。标题/分区变化只在同一`直播会话`内触发,开播/结束与直播间隙的字段变化不产生变化事件。

**直播会话 (live session)**:
从开始直播到结束直播的一次直播过程。每次开播都重置状态基线,不继承上一场直播的标题/分区等字段;标题/分区变化事件仅在会话进行中(前后状态均为 streaming)才触发。
_Avoid_: 直播间, 直播状态

**推送类型 (pushTypes)**:
插件推送哪些变化类型的通知。配置项(多选),四种类型各自独立开关。

**推送卡片 (PushCard)**:
直播变化通知的可视化载体,由 SVG 代码经渲染插件渲染成图片。包含主播信息与直播间快照摘要:头像、主播名、时间、事件副标题、封面、标题、分区、时长。
_Avoid_: 封面图 (封面是卡片内的一个元素,不是整张卡片)

**渲染回退 (render fallback)**:
渲染插件不可用或渲染失败时,通知自动退回纯文本形式的行为。卡片推送优先,回退兜底。

**同接数 (onlineNum)**:
直播间的实时观众人数,取自高能榜接口 getOnlineGoldRank 的 `onlineNum` 字段(免登录)。平均同接数为快照的时间加权平均,`Math.round` 取整显示。
_Avoid_: 在线人数(轮询接口 `get_status_info_by_uids` 的 `online` 字段是累积观看人数,不是同接数), 人气值

**累积观众 (accumulatedAudience)**:
本场直播开播以来的累计观看人数,取自轮询接口 `get_status_info_by_uids` 的 `online` 字段,每轮轮询覆盖更新;下播后重置为 0。推送文案为"看过 N人"。
_Avoid_: 同接数, 实时人数

**直播内容 (BiliLiveContent)**:
一场直播中一段连续的标题+分区区间。变更标题/分区时,变更前的内容作为历史记入 `liveContents`(startTime 为该内容开始时间,首个内容即开播时间;endTime 为变更时间);下播时把最后一段内容补录进历史。当前正在直播的内容不存于数组,即直播间信息的顶层字段。
_Avoid_: 直播记录, 内容列表

**同接快照 (BiliOnlineSnapshot)**:
开播后采集的单个时间点的同接数记录,形如 `[同接数, 秒级时间戳]`,存于直播间信息的 `onlineSnapshots`。开播事件立即抓首点,此后跟随直播轮询节拍采集(相邻请求间隔 500ms),随房间数据持久化,下播推送完成后清空。
_Avoid_: 同接历史(无独立历史,下播即清), 采集记录

**转发语 (forward commentary)**:
转发动态中转发者自己撰写的正文,与所转发的「原动态卡片」相对。可携带表情与「动态配图」。
_Avoid_: 转发文案, 转发内容, 原文

**原动态卡片 (origCard)**:
转发动态中被转发的那条动态经解析后的卡片:头部行(时间与作者)、正文行、表情映射与图片。转发语与原动态卡片以分隔线拼接输出。
_Avoid_: 原文, 引用内容

**动态配图 (dyn picture)**:
一条动态正文附带的图片,解析后合并为同一个图片列表。来源两处:OPUS 内容图片(`opus.pics`)与富文本图片节点(`rich_text_nodes` 中 `pics` 非空的节点,节点类型名不参与判定)。图片节点在正文里的占位文本(形如 `查看图片(6)`,N 为 B 站自算的图片数)不属于正文,解析时剔除,N 不参与任何逻辑。
_Avoid_: 封面(视频与直播封面是独立来源, 不是动态配图), 表情(表情经 `emojiMap` 内嵌正文, 不是动态配图)
